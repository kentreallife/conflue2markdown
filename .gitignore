# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# package-lock.json もしpackage.jsonを直接編集し、lockファイルはCI等で生成する場合はコメントアウトを外す
# yarn.lock もしyarn.lockを直接編集し、lockファイルはCI等で生成する場合はコメントアウトを外す

# Build Gulp
/build/
/dist/ 